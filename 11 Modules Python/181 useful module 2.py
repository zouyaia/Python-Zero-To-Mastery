import datetime
from time import time
from array import array

print(datetime.date.today())
print(time())
arr = array('i', [4, 7, 9]) # quicker bc of specific types stored initially 'i' = int
print(arr[1])